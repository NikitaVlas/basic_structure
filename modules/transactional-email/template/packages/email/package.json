{
  "name": "@{{PROJECT_NAME}}/email",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "worker": "node --env-file-if-exists=../../apps/api/.env --import tsx src/worker-cli.ts"
  },
  "dependencies": {
    "@{{PROJECT_NAME}}/observability": "*",
    "@{{PROJECT_NAME}}/database": "*",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
