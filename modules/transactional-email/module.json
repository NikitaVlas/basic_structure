{
  "$schema": "../../schemas/extension-manifest.schema.json",
  "schemaVersion": 2,
  "kind": "module",
  "id": "transactional-email",
  "version": "1.0.0",
  "starter": ">=0.1.0 <1.0.0",
  "capabilities": ["transactional-email","background-jobs"],
  "tags": ["backend","email"],
  "maturity": "stable",
  "name": "Transactional email",
  "description": "Typed verification and password-reset email templates with safe console and SMTP transports.",
  "files": "template",
  "requires": { "module:observability": "^1.0.0", "module:database-postgres": "^1.0.0" },
  "conflicts": [],
  "contributions": {
    "API_ENV": "contributions/api-env.txt"
  },
  "packageDependencies": {
    "apps/api/package.json": {
      "dependencies": { "@{{PROJECT_NAME}}/email": "*" }
    },
    "package.json": {
      "scripts": {
        "email:up": "docker compose -f compose.email.yml up -d --wait",
        "email:down": "docker compose -f compose.email.yml down",
        "email:worker": "npm run worker --workspace @{{PROJECT_NAME}}/email"
      }
    }
  }
}
