{
  "name": "@{{PROJECT_NAME}}/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "migrate": "node --env-file-if-exists=../../apps/api/.env --import tsx src/migrate-cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "test:integration": "node --env-file-if-exists=../../apps/api/.env --import tsx --test src/**/*.integration.test.ts"
  },
  "dependencies": { "pg": "^8.16.3" },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.15.5",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
